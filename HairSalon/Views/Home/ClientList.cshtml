@{
  Layout = "_Layout";
}

<h1>Salon 206</h1>

<h2>@Model["stylists"].GetFirstName() 's Clients</h2>

<ul>
  @foreach(var client in @Model["clients"])
  {
    <li>@client.GetLastName(), @client.GetFirstName()</li>
  }
</ul>

<a href="/">Home</a>
<a href="/stylists/stylists-all">All stylists</a>
